[project]
name = "hb-assignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.2",
    "python-dotenv>=1.2.1",
    "selenium>=4.41.0",
]
